# 개인 보유 종목 데이터 — public repo에 올리면 본인 포트폴리오가 노출됨
portfolio_data.json

# 로컬 데스크탑 실행용 파일 (GitHub Pages 웹 호스팅엔 불필요)
launcher.py
start-portfolio.bat
portfolio-launcher.exe
portfolio_data.zip

# 빌드 산출물 (PyInstaller)
build/
dist/
*.spec
__pycache__/
*.pyc

# Playwright 검증 캐시 (개발 시 생성됨)
.playwright-mcp/

# 내부 배포 전략 메모 (사용자에게 노출 불필요)
DEPLOYMENT.md

# 내부 설계/계획 문서 (로컬 보관용 — 공개 저장소엔 불필요)
docs/superpowers/

# 개발 중 임시 정적 서버 파일
.__server.js
.__c.js

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
